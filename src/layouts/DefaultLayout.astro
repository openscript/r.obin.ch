---
import "./layouts";
import "./DefaultLayout.css";
import { parseLocale } from "../utils/i18n";
import DocumentHead from "./groups/Head.astro";
import Header from "./groups/Header.astro";
import Footer from "./groups/Footer.astro";
import Ribbon from "./groups/Ribbon.astro";
import Meta from "./groups/Meta.astro";

type Props = {
  translations?: Record<string, string>;
};

const locale = parseLocale(Astro.params.locale);
const { translations } = Astro.props;
---

<html lang={locale}>
  <DocumentHead translations={translations}>
    <slot name="meta" slot="meta">
      <Meta />
    </slot>
  </DocumentHead>
  <body>
    <Ribbon translations={translations} />
    <Header />
    <main data-pagefind-body transition:animate="fade">
      <slot />
    </main>
    <Footer />
  </body>
</html>
